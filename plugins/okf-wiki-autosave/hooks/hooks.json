{
  "description": "Journal meaningful work into OKF worklog concepts after agent turns.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/okf_autosave.py\" --data-dir \"${CLAUDE_PLUGIN_DATA}\"",
            "timeout": 120,
            "statusMessage": "Journaling work into the OKF worklog"
          }
        ]
      }
    ]
  }
}
